// PT-BR SEO route: /voos/buscar
// Mirrors /flights/search — the next-intl middleware resolves pt-BR locale for
// Brazilian users landing on this canonical Portuguese URL path.
export { default } from '@/app/flights/search/page';
