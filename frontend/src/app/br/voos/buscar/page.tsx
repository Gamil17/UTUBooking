// PT-BR SEO route for the flight search results page.
// The next-intl middleware resolves pt-BR locale for /br/* paths automatically,
// so the same component renders in Portuguese without any extra wiring.
export { default } from '@/app/flights/search/page';
